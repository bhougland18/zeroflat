name: zeroflat
description: "Stateless Flutter UI renderer — turns FlatBuffer trees from Rust into Forui widgets via Rinf."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flat_buffers: ^23.5.26
  rinf: ^8.10.0
  tuple: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  forui: ^0.21.3
  zeroflat_forui:
    path: packages/zeroflat_forui

flutter:
