name: zeroflat_example
description: "Example application for the ZeroFlat renderer."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  zeroflat:
    path: ..
  zeroflat_forui:
    path: ../packages/zeroflat_forui
  rinf: ^8.10.0
  forui: ^0.21.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
